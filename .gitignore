# Node
node_modules/
dist/
*.log

# Entornos / secretos
.env

# Claves privadas (la de acceso SSH a la VM de Oracle se usa desde esta carpeta).
# Si una llega al repo, cualquiera con acceso al codigo entra al servidor: se
# ignoran por patron para que no dependa de acordarse del nombre del archivo.
*.key
*.pem

# Base de datos local
backend/data/

# Android
android-app/.gradle/
android-app/build/
android-app/app/build/
android-app/local.properties
android-app/.idea/
*.iml
android-app/captures/

# Sistema
.DS_Store

# Logs de start.sh
logs/

# Cache incremental de TypeScript
*.tsbuildinfo

# Build del frontend copiado para produccion local
backend/public/
