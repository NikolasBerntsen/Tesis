<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Escaneo del QR pegado en el dron -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Instantánea GPS del emparejamiento; COARSE va junto porque desde Android 12
         el diálogo de ubicación precisa se pide con las dos -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- required="false": el celular de vuelo siempre tiene cámara y GPS, pero así
         la app sigue siendo instalable en un emulador sin ellos -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_lanzador"
        android:roundIcon="@drawable/ic_lanzador"
        android:theme="@style/Theme.DronePatrol"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        tools:replace="android:usesCleartextTraffic,android:networkSecurityConfig">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FieldMenuActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>
</manifest>
