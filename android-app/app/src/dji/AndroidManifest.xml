<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- El RC-N2 se conecta al celular como accesorio USB. required="false" para que
         la app siga siendo instalable en dispositivos sin soporte de accesorios USB. -->
    <uses-feature android:name="android.hardware.usb.accessory" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application android:name="com.tesis.dronepatrol.dji.DjiApplication">
        <meta-data
            android:name="com.dji.sdk.API_KEY"
            android:value="${DJI_API_KEY}" />
    </application>
</manifest>
