# Plantilla del .env de producción. El archivo real vive SÓLO en la VM
# (el rsync del despliegue lo excluye) y lo genera deploy/deploy.sh la primera
# vez con un secreto aleatorio.

# Secreto para firmar los tokens JWT. Generarlo con: openssl rand -hex 32
JWT_SECRET=
